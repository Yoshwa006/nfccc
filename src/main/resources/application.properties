spring.application.name=nfc-rental
# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/nfc_rental
spring.datasource.username=root
spring.datasource.password=MashaAllah1996

# JDBC Driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Properties (JPA)
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# DDL Auto Configuration (Use 'update' in development, 'validate' in production)
spring.jpa.hibernate.ddl-auto=update

# Show SQL Queries in Console (Optional, for debugging)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


